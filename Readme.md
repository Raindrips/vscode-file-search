# Project File Search

A fast project file name searcher for VS Code.  
It offers search functionality similar to `Ctrl+P`, but limits the search to files within the current workspace.Features

- Search project files by name
- Configurable keybinding
- Ignores node_modules and other excluded folders

## Usage

Press `Ctrl+Alt+P` to open the file search panel.
